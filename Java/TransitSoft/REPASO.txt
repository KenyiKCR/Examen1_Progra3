Patron Singleton: Para tener una unica instancia de una clase, de 
Patrin Factory: Ejem Para facilitar la copnstuccion de instancias para ocultar/ abstraerme del nombre de la clase que la crea.
Patron Buikder: Se evitar tener varios contructores.
Patron DAO (BD Data Manager): Encapsula las operaciones de persistencia, te desacoplas del como.
Patron DTO: transfiere data. (Data Transfer Object)

Framework Agnostico: No conoce la capa de datos a la que se esta conectando

Polimorfismo, Interfaces funcionales:

Arquitectura Agnostica: Significa Si digo que se conecte a una nueca BD no tengo que over a copilar. Se conecte solito

1- La arquitectura SOFTiWA es (Arquitectura Cliente Servidor)
una
El que esta la medio se middwlare. El ciente se ejecuta en un browser (Safari, firefow, Chrone), Y por la parte del servidor este se ejecuta en un  servidor web (Tomcat,IIS,Glassfish).

Nosotros no estamos aplicando ninguna de los dos. Nosotros estamoa aplicando una aplicacion sevidera que corre con un servidor  web.

El middwlare, se implementara unsnto el PROTOCOLO http que implementa el procotoclo tcp/ip
El cliente envia info enviando un Request. Y el servidor respode usando un Respouse

Dentro del servidor tenemos al .html
Pagina estatica es nuestro html (Antes de la ejecucion su Contenido esta fijo). No depende de la tecnologia
Pagina Dinamica .Aspx (Cambia su contenido durante el momento de ejecucion). Depende de la tencologia.. Su uso .ASPX me limito a usar IIS.
.JSP me limito a tomcat o a Glassfish

El browser solo interpreta html. Es asi que el servirdor web hace todo su procesamiento con .aspx y retorna al cliente el .html

Cada vez que me conecto al servidor se genera un sesion. La sesion es uno de los elementos por los cuales puedo comunicar ciertos parametros a mi codigo. En mi sesion esta la IP del usasrio, la hora que lo vio.

El servidor web es lo que hace, el crea la instancia para la sesion de un usuario. 

Application Sesion ???

Ciclo de pida de contruccion de una pagina en aspx
Primero se ejecuta el contructor, luego ele evento Init y luego Load.


Sincronica y Asincronia:

Sincrono: Tengo un request y luego un response.
Asincrono: Para aplicarlo usaremos una tecnlogia llamada AJAX. (Osea que solo se ejecute una parte,
por ejemplo si le das like a un post de ig. Solo se actuliza el like no toda la pagina)
El evento post back. Es cuado una pagina ya esta cargada se vuelve a cargar en el servidor



BootStrap(Interfaz) -> Se trbaja con contenedores. Va de la mano con CSS y Html

JQuery  (JavaScript) -> 

Fonts Awesone (Iconos) -> 

Hemos hecho el mantenimiento de almacenes. El cual necesitaba 

Select sin predicado (where sin predicado (El predicado es el que va al lado del where))

Un select para 30k es enviable. 

proyeccion, las columnas del where.

Los javasrcip. se alamacena el servidor. En el cliente se ejecuta en Cliente


Al profe le importa para que sirve <- FIJA

Diferencias entre un back en C# y Java

-- Clase -------------------------------------------

Que pasa cuando no podemos lisrtar todos?
Es asi que el patron Master Detail aparece. La identidad maestro se relaciona con una entidad detalle (La cabecera y el detalle de la cabezera es un ejemplo de Master detail).

Tambien tienes otro tipo de mantenimiento. donde primero buscaras al usuario y luego lo mostraras. 

La pantalla de tipo modal, es la que se abre como un pop-up. 


Cart 

Aveces no copila porque no tenemos el metodo implementado del evento

Hay q darle soporte AJAX. Me permite refrescar ciertas partes de la pagina web sin refrescar toda la pagina.


Traer soluciones? CRUD del caso. SQL y SQL.
C#,Java. 
Script te lo darán